{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}

{% for product in products %}
<!--
rajouter if status=1
https://flowbite.com/docs/components/card/
https://tailwindui.com/components/ecommerce/components/product-lists
https://bootswatch.com/morph/
https://www.youtube.com/watch?v=3K6oBiQK8aA

 -->

<div class="w-full max-w-sm bg-white rounded-lg shadow-md dark:bg-gray-800 dark:border-gray-700">
    <a href="#">
        <img class="p-8 rounded-t-lg" src="https://www.genius-cbd.fr/wp-content/uploads/2021/11/Lemon_2-256x256.jpg" alt="{{ product.description }}" />
    </a>
    <div class="px-5 pb-5">
        <a href="#">
            <h5 class="text-xl font-semibold tracking-tight text-gray-900 dark:text-white"> {{ product.title }}</h5>
        </a>
       <h5 class="text-xl font-semibold tracking-tight text-gray-700 dark:text-white">Provenance: {{ product.madeIn }}</h5>
        <div class="flex items-center justify-between">
            <span class="text-3xl font-bold text-gray-900 dark:text-white">{{ product.price }} €</span>
            <a href="{{ path('app_article_show', {'id': product.id}) }}" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">Afficher</a>
        </div>
    </div>


            {% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
        {% endfor %}
{% endblock %}
{#----------------------------------------------------------------
{#

                    <a href="{{ path('app_product_show', {'id': product.id}) }}">show</a>
                    <a href="{{ path('app_product_edit', {'id': product.id}) }}">edit</a>

    <a href="{{ path('app_product_new') }}">Create new</a>



#}